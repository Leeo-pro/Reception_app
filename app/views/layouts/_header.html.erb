            <% unless user_signed_in? && current_user.blank? -%>
signed in
<% else -%>
not signed in
<% end-%>

before_filter :authenticate_user!

<% @user == current_user %>

<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <%= link_to "Reception App", root_path, id: "logo" %>
    <nav>
      <ul class="nav navbar-nav navbar-right">
        
        <li><%= link_to "ホーム", root_path %></li>
        <% if user_signed_in? %>
          <li><%= link_to "お知らせ", user_articles_path(current_user) %></li>
          <% if user_signed_in? && current_user.admin? %>
            <li><%= link_to "拠点登録", new_user_reservation_path(current_user) %></li>
          <% else%>
          <% end %>
          <li><%= link_to "全ての拠点", user_reservations_path(current_user) %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              メニュー <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%#= link_to "勤怠", current_user %></li>
              <li><%#= link_to "設定", edit_user_path(current_user) %></li>
              <li class="divider"></li>
              <li>
                <%= link_to "ログアウト", destroy_user_session_path, method: :delete %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "ログイン", user_session_path %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>